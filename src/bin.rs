extern crate numbars;

use numbars::dft;

pub fn main()
{
    let a: Vec<f64> = Vec::new();
    let b: Vec<f64> = Vec::new();
    let c: Vec<f64> = Vec::new();
    let d: Vec<f64> = Vec::new();

    dft::dft(a, b, c, d);
}
